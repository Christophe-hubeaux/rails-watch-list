<div class="container">
  <div class="mt-3">
    <h2>Fill the list form</h2>
  </div>
  <%= simple_form_for(@list) do |f| %>
    <%= f.input :name %>
    <%= f.input :photo, as: :file %>
    <%= f.submit %>
  <% end %>
</div>
